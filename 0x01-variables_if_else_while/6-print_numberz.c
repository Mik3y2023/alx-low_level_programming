#include <stdio.h>
/**
 * main - This is the main entry point
 *
 * return: always return 0 (success)
 */

int main(void)
{
int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
